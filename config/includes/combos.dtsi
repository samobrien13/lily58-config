/ {
    combos {
        compatible = "zmk,combos";
        combo_capsword = {
            compatible = "zmk,combo";
            key-positions = <28 31>;
            bindings = <&caps_word>;
            timeout-ms = <50>;
        };
    };
};
